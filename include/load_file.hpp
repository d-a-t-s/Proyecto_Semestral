#ifndef LOAD_FILE_HPP
#define LOAD_FILE_HPP

#include <string>

// Función para cargar un archivo de texto en un string (normalizado)
std::string load_file(const std::string& file_name);

#endif // LOAD_FILE_H